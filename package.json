{
  "name": "fractal-vite-ts",
  "version": "0.1.0",
  "dependencies": {
    "@frctl/fractal": "^1.5.15"
  },
  "scripts": {
    "fractal:start": "fractal start --watch --sync",
    "fractal:build": "fractal build",
    "start": "npm run build:client -- --watch",
    "build:client": "vite build --outDir dist/assets --config vite.config.ts"
  },
  "devDependencies": {
    "@frctl/mandelbrot": "^1.10.3",
    "sass": "^1.83.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
